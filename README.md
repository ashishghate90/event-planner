# Dependencies and Installation

Things you may want to cover:

* Ruby version : 3.2.2, Rails : 7.2.2

* System dependencies : Run 'bundle install' inside the project

* Database creation : Run 'rails db:setup' 

# Event-Planner:
- It's an simple application in which we have implemented the Authentication using Devise gem, Authenticated user can create the Event, and that will get reflect on Calender.
